<%= form_for(@interest_location) do |f| %>
  <% if @interest_location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@interest_location.errors.count, "error") %> prohibited this interest_location from being saved:</h2>

      <ul>
      <% @interest_location.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="wrapper">
    <div class="field">
      <div class="label">
      <%#= f.label :latitude %>
        <p>LATITUDE</p>
      </div>
      <div class="input">
        <%= f.text_field :latitude, :id => "lat", :readonly => true , placeholder: "Populating..." %>
      </div>
    </div>

    <div class="field">
      <div class="label">
      <%#= f.label :longitude %>
        <p>LONGITUDE</p>
      </div>
      <div class="input">
        <%= f.text_field :longitude, :id => "long", :readonly => true, placeholder: "...Please Wait" %>
      </div>  
    </div>
  </div>
<!--   <div class="field">
    <%#= f.label :radius %><br>
    <%#= f.number_field :radius %>
  </div> -->
  <div class="actions">
    <%= f.submit "Submit" %>
  </div>
<% end %>
