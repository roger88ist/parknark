<p id="notice"><%= notice %></p>

<div id="container-s">
  <div id="container-sl">
    <h1>Your Sightings:</h1>

    <table>
      <thead>
        <tr>
          <th>Day</th>
          <th>Latitude</th>
          <th>Longitude</th>
          <th>User</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @sightings.each do |sighting| %>
          <tr>
            <td><%= sighting.day %></td>
            <td><%= sighting.latitude %></td>
            <td><%= sighting.longitude %></td>
            <td><%= sighting.user.email %></td>
            <td><%= link_to 'Show', sighting %></td>
            <td><%= link_to 'Edit', edit_sighting_path(sighting) %></td>
            <td><%= link_to 'Destroy', sighting, method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>

  <div id="map">
    <h1>MAP GOES HERE</h1>
    <p>This map is the same map as the homepage map.</p>
    <p>But when the user enters a new sighting the map will add a marker representing the new sighting entered.</p>
  </div>

</div>
<br>

<p><%= link_to 'New Sighting', new_sighting_path %><p>
<p><%= link_to 'View Your Locations of Interest', interest_locations_path %></p>

