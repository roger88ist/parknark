<%= form_for(@sighting) do |f| %>
  <% if @sighting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sighting.errors.count, "error") %> prohibited this sighting from being saved:</h2>

      <ul>
      <% @sighting.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :day %><br>
    <%= f.text_field :day %>
  </div>
  <div class="field">
    <%= f.label :latitude %><br>
    <%= f.text_field :latitude, :id => "lat", :readonly => true %>
  </div>
  <div class="field">
    <%= f.label :longitude%><br>
    <%= f.text_field :longitude, :id => "long", :readonly => true  %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
